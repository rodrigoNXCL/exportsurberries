[build]
  command = "npm run build"
  publish = "dist"

# ⚠️ EXCEPCIÓN PRIMERO: Portal NO redirigir
[[redirects]]
  from = "/portal/*"
  to = "/portal/:splat"
  status = 200
  force = true

# Redirect general para el resto del sitio (DESPUÉS)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200